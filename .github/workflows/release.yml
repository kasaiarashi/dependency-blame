name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            artifact_name: dependency-blame
            asset_name: dependency-blame-linux-x64
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact_name: dependency-blame.exe
            asset_name: dependency-blame-windows-x64.exe
          - os: macos-latest
            target: x86_64-apple-darwin
            artifact_name: dependency-blame
            asset_name: dependency-blame-macos-x64
          - os: macos-latest
            target: aarch64-apple-darwin
            artifact_name: dependency-blame
            asset_name: dependency-blame-macos-arm64

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install dependencies (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: sudo apt-get update && sudo apt-get install -y libssl-dev pkg-config

      - name: Build
        run: cargo build --release --target ${{ matrix.target }}

      - name: Strip binary (Unix)
        if: matrix.os != 'windows-latest'
        run: strip target/${{ matrix.target }}/release/${{ matrix.artifact_name }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.asset_name }}
          path: target/${{ matrix.target }}/release/${{ matrix.artifact_name }}

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create ${{ github.ref_name }} \
            --title "${{ github.ref_name }} - Release" \
            --notes "See [CHANGELOG](https://github.com/${{ github.repository }}/blob/master/CHANGELOG.md) for details." \
            artifacts/dependency-blame-linux-x64/dependency-blame \
            artifacts/dependency-blame-windows-x64.exe/dependency-blame.exe \
            artifacts/dependency-blame-macos-x64/dependency-blame \
            artifacts/dependency-blame-macos-arm64/dependency-blame
